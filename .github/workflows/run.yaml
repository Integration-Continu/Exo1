name: Python application
on:
  push:
    branches: ["main"]

jobs:
run:
runs-on: ubuntu-latest
steps:
  - uses: actions/checkout@v3
  - uses: actions/setup-python@v4
    with:
      python-version: '3.10'
  - name: RUN
    run: |
      python sum.py

